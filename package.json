{
  "name": "turbo-linecount",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "node-addon-api": "^3.0.0",
    "node-gyp": "^7.0.0",
    "node-gyp-build": "^4.2.2"
  },
  "devDependencies": {
    "prebuildify": "^4.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp rebuild",
    "prebuild": "prebuildify --napi"
  },
  "author": "",
  "license": "ISC",
  "gypfile": true
}
